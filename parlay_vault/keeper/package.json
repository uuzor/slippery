{
  "name": "keeper",
  "version": "1.0.0",
  "description": "Parlay Vault Settlement Keeper for DeepBook Predict",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "bot": "npm run build && node --env-file=.env.local ./dist/bot.js",
    "start": "npm run build && node --env-file=.env.local ./dist/settle_slip.js",
    "smoke": "npm run build && node --env-file=.env.local ./dist/smoke_flow.js",
    "place-open": "npm run build && node --env-file=.env.local ./dist/place_open_slip.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mysten/sui": "^2.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
